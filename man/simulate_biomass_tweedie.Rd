% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_biomass_tweedie.R
\name{simulate_biomass_tweedie}
\alias{simulate_biomass_tweedie}
\title{Simula (o replica) biomasse da Tweedie in formato lungo}
\usage{
simulate_biomass_tweedie(
  data,
  params,
  response = "biomassa",
  species = "species",
  group_vars = c(NA),
  nsim = 100,
  n_min = 3,
  mu_min = 1e-08
)
}
\arguments{
\item{data}{Data frame lungo con le osservazioni (deve contenere \code{response} e \code{species}).}

\item{params}{Data frame con almeno \code{species}, \code{p_hat}, \code{phi_hat}.}

\item{response}{Nome della colonna risposta (es. "biomassa" o "B").}

\item{species}{Nome della colonna specie (es. "species").}

\item{group_vars}{Variabili di raggruppamento (default: c("area","period","year")).}

\item{nsim}{Numero di repliche simulate (o replicate) per gruppo.}

\item{n_min}{Soglia minima di osservazioni per simulare (default 3).}

\item{mu_min}{Soglia minima per \code{mu} quando si simula (evita 0 esatto), default 1e-8.}
}
\value{
Tibble lungo con le colonne originali + \code{iter} e \code{sim_biomassa}.
}
\description{
Per ogni gruppo (\code{group_vars} + \code{species}):
- se \code{n >= n_min}, simula da Tweedie con \code{mu =} osservazione riga-per-riga,
  \code{phi = phi_hat} e \code{power = p_hat} della specie;
- se \code{n < n_min}, NON simula ma replica i valori osservati per tutte le \code{nsim} iter.
}
